{
  "name": "fetch-images",
  "version": "1.0.0",
  "description": "Fetch some images from old image service",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "image-downloader": "^3.4.1",
    "rimraf": "^2.6.2"
  }
}
